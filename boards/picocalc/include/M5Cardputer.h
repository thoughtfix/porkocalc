#pragma once
// Porkocalc: route M5Stack Cardputer APIs to the PicoCalc compatibility layer from
// picocalc-esp32 (https://github.com/thoughtfix/picocalc-esp32). This folder is on the include
// path ONLY for [env:picocalc], so the Cardputer build still uses the real M5 libraries.
#include <PicoCalcCardputer.h>
